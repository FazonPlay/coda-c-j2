#include <stdlib.h>
#include <stdio.h>

int main(int argc, char const *argv[])
{
	double x = 7;
	double y = 12;
	double resultat = (x + y) / 2;
	printf("La moyenne de 12 et 7 est egale a %f \n", resultat);
	exit (0);

}